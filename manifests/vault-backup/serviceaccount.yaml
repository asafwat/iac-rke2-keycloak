# ServiceAccount used by the Vault snapshot CronJob.
# Bound on the Vault side to the `vault-snapshot` role via the Kubernetes
# auth method (configured by scripts/ps/init-vault.ps1 in Phase 6).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: vault-snapshot
  namespace: vault
