# Bootstrap ClusterIssuer - used once to mint the lab CA below.
# Apps never reference this directly.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned-bootstrap
spec:
  selfSigned: {}